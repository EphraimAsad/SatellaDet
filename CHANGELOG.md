# Changelog

## 0.2.1

- public package layout and installable CLI entry points;
- SatellaScore checkpoint selection and auxiliary best checkpoints;
- per-class validation metrics;
- ramped EMA behaviour for small datasets;
- configurable fixed image resolutions (including 640, 960, 1280);
- resolution-scaled target assignment;
- ONNX opset 18 export;
- privacy-safe runtime path metadata;
- checkpoint tracker output;
- generic trained-checkpoint ONNX export and verification commands.
